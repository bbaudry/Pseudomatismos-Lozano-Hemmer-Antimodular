#include <Carbon/Carbon.h> 
#include <Cocoa/Cocoa.h>

#define IMAGE_W (640)
#define IMAGE_H (480)

#define MAX_IMAGE_W (1024)
#define MAX_IMAGE_H (768)

#define SEASONS (11)

//typedef enum _BackGndMode { bmRaw = 0, bmBackGnd, bmSubtracted } BackGndMode;
typedef enum _CameraMode { cmStopped = 0, cmPaused, cmRunning } CameraMode;
typedef enum _RunMode { rmLive = 0, rmPlayBack} RunMode;

typedef char PlayBackFolder[80]; 

//typedef unsigned char byte;
typedef unsigned short word;

