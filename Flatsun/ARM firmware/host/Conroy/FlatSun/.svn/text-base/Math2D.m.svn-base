#import "Math2D.h"

@implementation Math2D


+ (float) radToDeg : (float) rads
{
  float result = 180 * (rads / PI);
  return result;
}

+ (float) degToRad : (float) degs
{  
  float result = PI * degs / 180;
  return result;
}

@end
